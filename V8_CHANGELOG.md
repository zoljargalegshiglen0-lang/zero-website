# WINGS V8 Premium Refresh

## Updated
- Servers page rebuilt into a compact live-network layout inspired by premium CS2 server browsers without copying the references.
- Skinchanger top-level UI rebuilt into a tighter loadout studio while keeping the existing loadout / craft / sticker / charm functions.
- Removed monochrome filtering from skins and media so real cosmetic colors stay visible.
- Admin page rebuilt into a different compact owner-control layout with metrics, integrations and quick links.
- Reduced oversized headings and unnecessary text.
- Header/sidebar simplified.

## CS2 plugin readiness
- `/api/loadout` remains the website save/sync path.
- `CS2_BRIDGE_URL` and optional `CS2_BRIDGE_TOKEN` are supported.
- See `COMMUNITY_INTEGRATION.md` for expected bridge endpoints.

## Validation
- Syntax transpile check passed for the main modified TSX files.
